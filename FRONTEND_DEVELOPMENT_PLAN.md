# План разработки фронтенда — Подробная дорожная карта

> Этот документ содержит подробный план развития React фронтенда в соответствии с требованиями PROMPT.md и архитектурой бэкенда.

---

## Общая стратегия

**Основной принцип:** Frontend разрабатывается **итеративно параллельно с Backend**, с привязкой к завершённым API endpoints.

**Технологический стек (финальный):**
- React 18 + TypeScript
- React Router v6
- Context API + useReducer (глобальное состояние)
- CSS Modules + Sass (стили)
- Canvas API (визуализация турнирных сеток и графиков)
- Drag & Drop (HTML5 API)
- JWT для хранения токенов (localStorage + защита)
- Axios для HTTP-запросов
- Jest + React Testing Library для тестирования

**Структура проекта:**
```
frontend/
├── src/
│   ├── components/          # React компоненты
│   │   ├── common/          # Переиспользуемые компоненты
│   │   ├── layout/          # Layout компоненты
│   │   ├── auth/            # Auth-related компоненты
│   │   ├── club/            # Club management
│   │   ├── athletes/        # Athletes management
│   │   ├── documents/       # Document management
│   │   ├── finances/        # Financial module
│   │   ├── attendance/      # Attendance tracking
│   │   ├── tournaments/     # Tournament management
│   │   └── analytics/       # Analytics & reporting
│   ├── context/             # Context API + useReducer
│   ├── hooks/               # Custom React hooks
│   ├── types/               # TypeScript types/interfaces
│   ├── services/            # API calls (axios wrapper)
│   ├── utils/               # Utility functions
│   ├── styles/              # Global styles + variables
│   ├── pages/               # Page components (routes)
│   ├── App.tsx              # Main component
│   └── main.tsx             # Entry point
├── tests/                    # Test files (mirroring src/)
├── public/                   # Static assets
├── package.json
├── tsconfig.json
├── vite.config.ts           # Vite configuration
└── index.html
```

---

## Фазы разработки

### **Фаза 0: Инициализация и базовая структура (Stage 0 Frontend)**

**Сроки:** 1-2 дня  
**Цель:** Подготовить проект к разработке функциональности

#### Задачи:
1. ✅ Инициализировать React + Vite + TypeScript в папке `frontend/`
2. ✅ Настроить React Router v6 с базовой структурой маршрутов
3. ✅ Установить зависимости: Axios, Sass
4. ✅ Создать базовую Context API + useReducer для глобального состояния (auth, ui)
5. ✅ Создать CSS Modules + Sass структуру
6. ✅ Настроить TypeScript (strict mode)
7. ✅ Создать простую главную страницу с навигацией
8. ✅ Настроить proxy для API запросов (http://localhost:8081)

#### Входные критерии:
- Backend запущен и работает на http://localhost:8081
- API endpoints задокументированы

#### Выходные критерии:
- `npm run dev` в frontend работает без ошибок
- React Router загружает основные маршруты
- Context API инициализирована
- TypeScript компилируется без ошибок

#### Артефакты:
- `frontend/src/` структура
- `frontend/tsconfig.json` (strict mode)
- `frontend/vite.config.ts` (proxy настроена)
- `App.tsx` с базовыми маршрутами
- Context providers (AuthContext, UIContext)

---

### **Фаза 1: Аутентификация и базовая навигация (Stage 1 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Stage 1 Backend (DONE ✅)

#### Задачи:

##### 1.1 Компоненты аутентификации
- `pages/Login.tsx` — форма входа
- `pages/Register.tsx` — форма принятия приглашения (по токену)
- `components/auth/LoginForm.tsx` — логин-форма
- `components/auth/RegisterForm.tsx` — регистрация-форма
- `components/auth/ProtectedRoute.tsx` — защищённые маршруты по ролям

##### 1.2 Auth Context & Hooks
- `context/AuthContext.tsx` — управление аутентификацией
  - Состояние: currentUser, tokens, isLoading, error
  - Action: LOGIN, LOGOUT, REFRESH_TOKEN, SET_USER, SET_ERROR
- `hooks/useAuth.ts` — custom hook для доступа к auth
- `hooks/useProtectedRoute.ts` — проверка ролей
- `services/authService.ts` — API calls для auth (login, logout, refresh)

##### 1.3 Сохранение токенов
- localStorage для access/refresh токенов
- Функция refresh при истечении access token
- Logout при ошибке refresh
- Обработка ошибок (401 Unauthorized)

##### 1.4 Базовая навигация
- `components/layout/Header.tsx` — заголовок с меню
- `components/layout/Sidebar.tsx` — боковая панель навигации
- `components/layout/MainLayout.tsx` — основной лейаут
- Пункты меню по ролям (superadmin, admin, coach, judge, athlete, parent)

##### 1.5 Страницы
- `pages/Login.tsx` — главная страница входа
- `pages/Register.tsx` — страница принятия приглашения (с параметром ?token=...)
- `pages/Dashboard.tsx` — главная страница (разные для разных ролей)
- `pages/NotFound.tsx` — 404 страница

#### Входные критерии:
- Backend endpoints: POST /auth/login, POST /auth/refresh, POST /auth/logout работают
- Backend endpoints: POST /users/invite/accept работают

#### Выходные критерии:
- Пользователь может залогиниться через форму
- Пользователь может принять приглашение по токену
- Токены сохраняются и используются в запросах
- Защищённые маршруты работают по ролям
- Логаут работает корректно
- JWT refresh работает прозрачно

#### Тесты:
- Unit: AuthContext reducer логика
- Integration: Login flow
- Integration: Token refresh
- Компонентные: LoginForm, RegisterForm

#### Артефакты:
- `src/pages/Login.tsx`, `Register.tsx`, `Dashboard.tsx`
- `src/components/auth/*`
- `src/context/AuthContext.tsx`
- `src/services/authService.ts`
- `src/hooks/useAuth.ts`, `useProtectedRoute.ts`
- Тесты в `tests/`

---

### **Фаза 2: Управление клубом (Stage 2 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Stage 1.5 Backend (Club CRUD + multitenancy)

#### Задачи:

##### 2.1 Выбор клуба и контекст клуба
- `context/ClubContext.tsx` — управление текущим клубом
  - Состояние: selectedClub, clubs, isLoading
  - Action: SET_CLUB, SET_CLUBS, ADD_CLUB
- `hooks/useClub.ts` — custom hook для доступа к текущему клубу
- `services/clubService.ts` — API calls (GET /clubs, POST /clubs, GET /clubs/:id, PATCH /clubs/:id)
- Middleware в App.tsx для проверки выбора клуба

##### 2.2 Страница списка клубов
- `pages/Clubs.tsx` — список всех клубов (для superadmin)
- `components/club/ClubList.tsx` — таблица/карточки клубов
- `components/club/ClubCard.tsx` — карточка одного клуба
- Фильтрация, поиск, сортировка

##### 2.3 Страница создания клуба
- `pages/CreateClub.tsx` — форма создания
- `components/club/CreateClubForm.tsx` — форма с валидацией
- Wizard-style (несколько шагов) или одноэтапная форма

##### 2.4 Страница деталей клуба
- `pages/ClubDetails.tsx` — информация о клубе (просмотр/редактирование)
- `components/club/ClubDetailsComponent.tsx`
- Редактирование для admin/superadmin
- Просмотр для остальных

##### 2.5 Управление пользователями клуба
- `pages/ClubUsers.tsx` — список пользователей
- `components/club/UserList.tsx` — таблица пользователей
- `components/club/InviteUserForm.tsx` — форма приглашения
- `services/userService.ts` — API calls для пользователей

##### 2.6 Роли и доступы
- Таблица с ролями (выбор при приглашении)
- Описание прав каждой роли
- Фильтрация пользователей по ролям

#### Входные критерии:
- Backend endpoints: GET /clubs, POST /clubs, GET /clubs/:id, PATCH /clubs/:id
- Backend endpoints: GET /users, POST /users/invite
- Auth Context работает

#### Выходные критерии:
- Пользователь может создать клуб
- Пользователь может пригласить в клуб
- Список пользователей отображается
- Роли отображаются и работают корректно
- После выбора клуба он устанавливается в context

#### Артефакты:
- `src/pages/Clubs.tsx`, `CreateClub.tsx`, `ClubDetails.tsx`, `ClubUsers.tsx`
- `src/components/club/*`
- `src/context/ClubContext.tsx`
- `src/services/clubService.ts`, `userService.ts`
- Тесты

---

### **Фаза 3: Управление спортсменами и группами (Stage 2 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Stage 1.5 Backend (Athletes/Groups CRUD)

#### Задачи:

##### 3.1 Список спортсменов
- `pages/Athletes.tsx` — главная страница спортсменов
- `components/athletes/AthleteList.tsx` — таблица спортсменов
- `components/athletes/AthleteCard.tsx` — карточка спортсмена
- Фильтрация по группе, статусу, полу
- Поиск по имени/фамилии
- Экспорт списка (CSV)

##### 3.2 Создание/редактирование спортсмена
- `pages/CreateAthlete.tsx` — создание нового спортсмена
- `pages/EditAthlete.tsx` — редактирование спортсмена
- `components/athletes/AthleteForm.tsx` — форма с валидацией
  - Поля: имя, фамилия, дата рождения, пол, вес, рост, статус
  - Загрузка фото (аватара)

##### 3.3 Профиль спортсмена
- `pages/AthleteProfile.tsx` — полный профиль
- `components/athletes/AthleteDetails.tsx` — детали
- `components/athletes/AthleteStats.tsx` — статистика
- История выступлений (матчи, турниры)
- История изменений веса

##### 3.4 Управление группами
- `pages/Groups.tsx` — список групп
- `pages/CreateGroup.tsx` — создание группы
- `pages/GroupDetails.tsx` — детали группы с составом
- `components/groups/GroupForm.tsx` — форма создания/редактирования
- Добавление/удаление спортсменов из группы

##### 3.5 Родительские привязки
- `components/athletes/ParentAssignment.tsx` — связь спортсменов с родителями
- Таблица связей
- Добавление/удаление родителей для спортсмена

##### 3.6 Service & Context
- `services/athleteService.ts` — API calls
- `services/groupService.ts` — API calls
- `context/AthleteContext.tsx` (опционально, если нужна глобальная кэширование)

#### Входные критерии:
- Backend endpoints: GET/POST /athletes, GET/PATCH /athletes/:id
- Backend endpoints: GET/POST /groups, GET /groups/:id
- Auth & Club Context работают

#### Выходные критерии:
- Спортсмены отображаются в таблице
- Можно создавать/редактировать спортсменов
- Профиль спортсмена с историей доступен
- Группы создаются и управляются
- Связи с родителями работают

#### Артефакты:
- `src/pages/Athletes.tsx`, `CreateAthlete.tsx`, `EditAthlete.tsx`, `AthleteProfile.tsx`
- `src/pages/Groups.tsx`, `CreateGroup.tsx`, `GroupDetails.tsx`
- `src/components/athletes/*`, `src/components/groups/*`
- `src/services/athleteService.ts`, `groupService.ts`

---

### **Фаза 4: Документооборот (Stage 3 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Stage 2 Backend (Documents endpoints)

#### Задачи:

##### 4.1 Загрузка документов
- `pages/Documents.tsx` — управление документами
- `components/documents/DocumentUpload.tsx` — форма загрузки
  - Валидация типов (MIME)
  - Валидация размера
  - Категория документа (медицинский, страховой, юридический и т.д.)
  - Привязка к спортсмену/клубу
- `services/documentService.ts` — API calls

##### 4.2 Список документов
- `components/documents/DocumentList.tsx` — таблица документов
- Фильтрация по категориям, статусу, дате
- Сортировка по дате, названию

##### 4.3 Предпросмотр и управление
- `components/documents/DocumentPreview.tsx` — встроенный просмотр (если возможно)
- Скачивание документа
- Удаление документа (с подтверждением)
- История версий документа

##### 4.4 Контроль сроков
- `components/documents/DocumentExpiry.tsx` — таблица со сроками действия
- Выделение истёкших (красный цвет)
- Выделение скоро истекающих (жёлтый цвет)
- Уведомления (если бэкенд их поддерживает)

##### 4.5 Интеграция со спортсменами
- Документы отображаются в профиле спортсмена
- Быстрая загрузка документов из профиля

#### Входные критерии:
- Backend endpoints: POST /documents (upload), GET /documents, DELETE /documents/:id
- Multipart form-data поддержка в API

#### Выходные критерии:
- Документы загружаются и отображаются
- Предпросмотр работает (если поддерживается)
- Контроль сроков работает
- История версий отображается

#### Артефакты:
- `src/pages/Documents.tsx`
- `src/components/documents/*`
- `src/services/documentService.ts`

---

### **Фаза 5: Журнал посещений (Stage 3 Frontend)**

**Сроки:** 2-3 дня  
**Зависимость:** Stage 2 Backend (Attendance endpoints)

#### Задачи:

##### 5.1 Календарный интерфейс
- `pages/Attendance.tsx` — главная страница
- `components/attendance/AttendanceCalendar.tsx` — календарь (grid с днями)
- Выбор месяца/года
- Клик на день открывает день

##### 5.2 Отметка посещений
- `components/attendance/DayAttendance.tsx` — отметка посещений в день
  - Список спортсменов в группe
  - Чекбоксы для присутствия/отсутствия
  - Причины отсутствия (болезнь, отпуск, дисквалификация)
- `components/attendance/AttendanceForm.tsx` — форма редактирования

##### 5.3 Статистика посещаемости
- `components/attendance/AttendanceStats.tsx` — статистика спортсмена
  - Процент посещений
  - Активность (в тренировочном периоде)
  - Периоды отсутствия

##### 5.4 Интеграция с расписанием
- Если бэкенд поддерживает schedule, показывать дни с тренировками
- Отмечать дни с турнирами

##### 5.5 Service
- `services/attendanceService.ts` — API calls

#### Входные критерии:
- Backend endpoints: GET/POST /attendance, GET /attendance/stats

#### Выходные критерии:
- Календарь отображается
- Посещения можно отмечать
- Статистика вычисляется

#### Артефакты:
- `src/pages/Attendance.tsx`
- `src/components/attendance/*`
- `src/services/attendanceService.ts`

---

### **Фаза 6: Финансовая система (Stage 4 Frontend, часть 1)**

**Сроки:** 4-5 дней  
**Зависимость:** Stage 2 Backend (Finance endpoints)

#### Задачи:

##### 6.1 Платежи спортсменов
- `pages/Payments.tsx` — управление платежами
- `components/finances/PaymentList.tsx` — таблица платежей
  - Спортсмен, сумма, статус, дата платежа
  - Фильтрация по статусу (ожидается, частично оплачен, оплачен, просрочен)
  - Сортировка
- `components/finances/PaymentDetails.tsx` — детали платежа
  - История платежей
  - Загруженные чеки
  - Комментарии

##### 6.2 Подтверждение оплат
- `components/finances/PaymentConfirmation.tsx` — форма подтверждения (для администратора)
- Загрузка сканов чеков
- Подтверждение оплаты (полное/частичное)
- История подтверждений

##### 6.3 Визуализация задолженности
- `components/finances/DebtVisualization.tsx` — таблица должников
  - Красное выделение просроченных платежей
  - Сумма задолженности по спортсмену

##### 6.4 Выплаты тренерам
- `pages/CoachPayouts.tsx` — выплаты тренерам
- `components/finances/CoachPaymentList.tsx` — таблица выплат
- `components/finances/CoachPayoutRules.tsx` — конфигурация правил
  - Фиксированная ставка
  - Оплата за активного спортсмена
  - Курс трансформации в рубли (если нужно)
- Автоматический расчет
- История выплат

##### 6.5 Расходы клуба
- `pages/Expenses.tsx` — учет расходов
- `components/finances/ExpenseList.tsx` — таблица расходов
- `components/finances/ExpenseForm.tsx` — создание расхода
  - Категория, сумма, дата, описание
  - Привязка документов
- **Сводные отчеты**

##### 6.6 Financial Reports
- `components/finances/FinancialReport.tsx` — сводный отчёт
  - Доходы (платежи спортсменов)
  - Расходы (выплаты тренерам, расходы)
  - Чистый доход
  - Графики по месяцам

##### 6.7 Service
- `services/paymentService.ts`
- `services/coachPayoutService.ts`
- `services/expenseService.ts`
- `services/financialReportService.ts`

#### Входные критерии:
- Backend endpoints: GET/POST /payments, PATCH /payments/:id (confirm)
- Backend endpoints: GET/POST /coach-payouts, GET /expenses, POST /expenses
- Backend endpoints: GET /financial-reports

#### Выходные критерии:
- Платежи отображаются с правильными статусами
- Администратор может подтверждать платежи
- Выплаты тренерам рассчитываются автоматически
- Финансовые отчеты доступны

#### Артефакты:
- `src/pages/Payments.tsx`, `CoachPayouts.tsx`, `Expenses.tsx`
- `src/components/finances/*`
- `src/services/paymentService.ts`, `coachPayoutService.ts`, `expenseService.ts`

---

### **Фаза 7: Турнирная система — часть 1 (Основы) (Stage 5 Frontend)**

**Сроки:** 5-7 дней  
**Зависимость:** Stage 2 Backend (Tournament CRUD + Bracket generation)  
**Критичность:** ОЧЕНЬ ВЫСОКАЯ (один из основных модулей)

#### Задачи:

##### 7.1 Создание и управление турниром
- `pages/Tournaments.tsx` — список турниров
- `components/tournaments/TournamentList.tsx` — таблица турниров
- `pages/CreateTournament.tsx` — создание турнира
- `components/tournaments/TournamentForm.tsx` — форма создания
  - Название, дата, место
  - Категории (вес, возраст, пол)
  - Систем проведения (олимпийская, круговая)
  - Максимум участников

##### 7.2 Регистрация участников
- `pages/TournamentParticipants.tsx` — управление участниками
- `components/tournaments/ParticipantList.tsx` — таблица участников
- `components/tournaments/RegisterAthlete.tsx` — форма регистрации спортсмена
  - Выбор спортсмена
  - Выбор категории (вес, пол, возраст)
  - Валидация документов (медицинские справки и т.д.)
- Подтверждение явки перед началом турнира

##### 7.3 Визуализация сетки (самая критичная часть!)
- `components/tournaments/BracketVisualization.tsx` — **главный компонент**
  - Canvas-based рендеринг сетки
  - Поддержка олимпийской системы (бракет)
  - Отображение участников, bye
  - Отображение результатов в реал-тайме
  - Масштабирование и скролл для больших сеток

##### 7.4 Ввод результатов матчей
- `pages/TournamentMatches.tsx` — управление матчами
- `components/tournaments/MatchDetails.tsx` — детали матча
  - Участники (слева/справа)
  - Результат (победитель)
  - Время матча
  - Техники (ippon, waza-ari)
  - Штрафы (shido)
  - Видео-ссылка
  - Комментарии тренеров
- `components/tournaments/InputMatchResult.tsx` — форма ввода результата
- Автоматическое продвижение победителя в следующий раунд
- **Real-time обновление сетки** (WebSocket или polling)

##### 7.5 Drag & Drop для жеребьевки
- `components/tournaments/DraggableBracket.tsx` — вариант сетки с drag-and-drop
- Возможность ручной перестановки участников перед турниром
- Ограничения (один участник не может быть в двух местах)

##### 7.6 Экспорт и печать
- `components/tournaments/ExportBracket.tsx`
- Экспорт сетки в PDF (canvas-to-PDF)
- Экспорт результатов в CSV/Excel
- Печать сетки (оптимизированная для печати)

##### 7.7 Service & Context
- `services/tournamentService.ts` — API calls (CRUD турниров)
- `services/matchService.ts` — API calls (результаты матчей)
- `context/TournamentContext.tsx` (опционально) — кэширование текущего турнира

##### 7.8 Utilities
- `utils/bracketAlgorithm.ts` — локальное отображение сетки (дублирует бэкенд логику)
- `utils/canvasDrawing.ts` — функции для рисования на canvas

#### Входные критерии:
- Backend endpoints: GET/POST /tournaments, GET /tournaments/:id, PATCH /tournaments/:id
- Backend endpoints: GET /tournaments/:id/matches, POST /matches/:id/result
- Backend endpoint: GET /tournaments/:id/bracket (вернёт структуру сетки)

#### Выходные критерии:
- Турнир создаётся с категориями
- Спортсмены регистрируются
- Сетка отображается корректно (олимпийская система)
- Результаты вводятся
- Сетка обновляется в реал-тайме
- Сетка экспортируется в PDF
- Drag-and-drop жеребьевки работает (если разрешено)

#### Критические компоненты (требуют особого внимания):
1. **Canvas Rendering** — производительность, масштабирование
2. **Real-time Updates** — синхронизация с сервером
3. **Drag-and-Drop** — удобство UX
4. **PDF Export** — корректность отрисовки

#### Артефакты:
- `src/pages/Tournaments.tsx`, `CreateTournament.tsx`, `TournamentParticipants.tsx`, `TournamentMatches.tsx`
- `src/components/tournaments/*` (BracketVisualization.tsx — критичный)
- `src/services/tournamentService.ts`, `matchService.ts`
- `src/utils/bracketAlgorithm.ts`, `canvasDrawing.ts`
- Canvas-to-PDF library (canvas2pdf или similar)

---

### **Фаза 8: Аналитика и отчетность (Stage 6 Frontend)**

**Сроки:** 4-5 дней  
**Зависимость:** Stage 3 Backend (Analytics endpoints) + Турнирная система

#### Задачи:

##### 8.1 Аналитика спортсмена
- `pages/AthleteAnalytics.tsx` — аналитика спортсмена (из профиля)
- `components/analytics/WeightChart.tsx` — график динамики веса
  - Canvas-based линейный график
  - Период (месяц, квартал, год)
  - Целевой вес (если установлен)
- `components/analytics/PerformanceStats.tsx` — статистика выступлений
  - Количество побед/поражений
  - Процент побед по категориям
  - Лучшие результаты

##### 8.2 Радарная диаграмма бойца
- `components/analytics/FighterRadar.tsx` — Canvas-based радар
  - Оси: атакующий потенциал, защита, физ. подготовка, тактика, психология
  - Данные из турниров и тренировок
  - Сравнение с другими спортсменами (если нужно)

##### 8.3 Тепловая карта техник
- `components/analytics/TechniqueHeatmap.tsx` — Canvas-based тепловая карта
  - Статистика используемых техник
  - Успешность техник (ippon, waza-ari%)
  - Как текст/таблица или визуально

##### 8.4 Клубная аналитика
- `pages/ClubAnalytics.tsx` — аналитика по клубу
- `components/analytics/AthleteComparison.tsx` — сравнение спортсменов
  - Таблица рейтинга спортсменов (по победам, очкам)
  - Фильтрация по возрасту, весу, полу
- `components/analytics/CoachEffectiveness.tsx` — эффективность тренеров
  - Количество успешных спортсменов
  - Средний рейтинг подопечных
  - Статистика по прошедшим турнирам

##### 8.5 Финансовые отчеты (повтор/расширение)
- `components/analytics/FinancialDashboard.tsx` — дашборд финансов
  - Графики доходов/расходов по месяцам
  - Топ-10 должников
  - Тренд доходов

##### 8.6 Canvas-based графики
- Использование Canvas API напрямую или библиотеки типа:
  - Chart.js (простая библиотека для графиков)
  - Или собственные Canvas рисунки (для более кастомных)
- Экспорт графиков в PNG/PDF

##### 8.7 Service
- `services/analyticsService.ts` — API calls для аналитики

#### Входные критерии:
- Backend endpoints: GET /athletes/:id/analytics, GET /clubs/:id/analytics
- Backend endpoints: GET /analytics/tournament-stats и т.д.

#### Выходные критерии:
- Графики отображаются корректно
- Данные актуальны
- Сравнение спортсменов работает
- Экспорт графиков доступен

#### Артефакты:
- `src/pages/AthleteAnalytics.tsx`, `ClubAnalytics.tsx`
- `src/components/analytics/*`
- `src/services/analyticsService.ts`
- Chart.js или другая графическая библиотека

---

### **Фаза 9: Системные функции и дополнения (Stage 7 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Остальные фазы

#### Задачи:

##### 9.1 Аудит и логирование
- `pages/AuditLog.tsx` — логи действий
- `components/system/AuditLogTable.tsx` — таблица логов (для superadmin)
  - Пользователь, действие, ресурс, время, результат

##### 9.2 Экспорт данных
- `components/system/DataExport.tsx` — компонент экспорта
  - Экспорт клуба в JSON/CSV
  - Экспорт отчетов в PDF
  - Резервное копирование (инициирование на бэкенде)

##### 9.3 Уведомления
- `components/common/NotificationCenter.tsx` — центр уведомлений
  - Панель уведомлений (колокольчик в заголовке)
  - Список уведомлений
  - Отметить как прочитано
  - Web Push API (если поддерживается бэкенда)
- `services/notificationService.ts` — API calls

##### 9.4 Настройки системы (для админа)
- `pages/SystemSettings.tsx` — настройки
- `components/system/ValidationSettings.tsx` — настройки валидации файлов
- `components/system/NotificationSettings.tsx` — настройки уведомлений
- `components/system/FinancialRulesSettings.tsx` — настройки финансовых правил

##### 9.5 Поиск по всему приложению
- `components/common/GlobalSearch.tsx` — поле глобального поиска
  - Поиск по спортсменам, документам, турнирам
  - Быстрая навигация на результат

##### 9.6 Клавиатурные скорлауты
- Горячие клавиши для основных операций (Ctrl+S для сохранения и т.д.)
- Документация в справке

#### Артефакты:
- `src/pages/AuditLog.tsx`, `SystemSettings.tsx`
- `src/components/system/*`, `components/common/*`
- `src/services/notificationService.ts`

---

### **Фаза 10: Тестирование и оптимизация (Stage 8 Frontend)**

**Сроки:** 3-4 дня  
**Зависимость:** Все функциональные фазы

#### Задачи:

##### 10.1 Unit тесты
- Каждый компонент должен иметь тесты
- Context, hooks, utils тесты
- Coverage: минимум 70%

##### 10.2 Integration тесты
- Auth flow
- CRUD операции
- Фильтрация и поиск
- Форма валидация

##### 10.3 E2E тесты (опционально)
- Cypress или Playwright
- Основные сценарии использования

##### 10.4 Производительность
- Ленивая загрузка компонентов (React.lazy + Suspense)
- Оптимизация re-renders (useMemo, useCallback)
- Код-сплитинг по маршрутам
- Кэширование API responses

##### 10.5 Доступность (a11y)
- ARIA labels для компонентов
- Клавиатурная навигация
- Контрастные цвета
- Масштабируемость текста

##### 10.6 Responsive дизайн
- Mobile-first подход
- Media queries для разных размеров экрана
- Touch-friendly элементы (размер > 44px)

#### Артефакты:
- `tests/` покрытие компонентов
- Performance report
- Accessibility audit report

---

## Зависимости между фазами

```
Stage 0 (Инициализация)
    ↓
Stage 1 (Аутентификация) ←─ Backend Stage 1
    ↓
┌─ Stage 2 (Управление клубом, спортсмены) ←─ Backend Stage 1.5
│   ├─ Stage 3 (Документооборот) ←─ Backend Stage 2
│   ├─ Stage 4 (Финансы) ←─ Backend Stage 2
│   └─ Stage 5 (Журнал посещений) ←─ Backend Stage 2
│
└─ Stage 5 (Турнирная система) ←─ Backend Stage 2 + Stage 3
    └─ Stage 6 (Аналитика) ←─ Backend Stage 3
    
Stage 7 (Системные функции) ←─ Backend Stage 4
Stage 8 (Тестирование) ← Все предыдущие
```

---

## Спецификация компонентов (Часто используемые)

### Common Components (переиспользуемые)

```typescript
// components/common/
├── Button.tsx          // Базовая кнопка
├── Input.tsx           // Текстовое поле
├── Select.tsx          // Выпадающий список
├── DatePicker.tsx      // Выбор даты
├── Table.tsx           // Таблица с сортировкой, пагинацией
├── Modal.tsx           // Модальное окно
├── Toast.tsx           // Уведомления (toast)
├── Badge.tsx           // Статус-бейдж
├── Avatar.tsx          // Аватар пользователя/спортсмена
├── Card.tsx            // Карточка контана
├── Loading.tsx         // Спиннер загрузки
├── Pagination.tsx      // Пагинация таблицы
├── GlobalSearch.tsx    // Глобальный поиск
├── NotificationCenter.tsx // Центр уведомлений
└── ErrorBoundary.tsx   // Error boundary
```

### Layout Components

```typescript
// components/layout/
├── Header.tsx          // Заголовок с логотипом и меню
├── Sidebar.tsx         // Левая navegation
├── MainLayout.tsx      // Основной лейаут (Header + Sidebar + Content)
├── Footer.tsx          // Подвал (если нужен)
└── Breadcrumbs.tsx     // Хлебные крошки навигации
```

---

## Технические требования к кодованию

### TypeScript
- Strict mode включен
- Каждый компонент должен иметь типы props
- Interfaces для моделей данных

### Styles (CSS Modules + Sass)
```
components/
├── Button/
│   ├── Button.tsx
│   ├── Button.module.scss      // Стили для компонента
│   └── Button.test.tsx
```

### Структура файла компонента
```typescript
// components/MyComponent.tsx
import React from 'react';
import { SomeInterface } from '@/types';
import styles from './MyComponent.module.scss';

interface MyComponentProps {
  title: string;
  onAction: () => void;
}

export const MyComponent: React.FC<MyComponentProps> = ({ title, onAction }) => {
  return (
    <div className={styles.container}>
      <h1>{title}</h1>
      <button onClick={onAction}>Action</button>
    </div>
  );
};

export default MyComponent;
```

### API Services Pattern
```typescript
// services/apiService.ts
import axios, { AxiosInstance } from 'axios';

const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:8081';

const apiClient: AxiosInstance = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Интерцептор для добавления токена
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default apiClient;
```

### Custom Hooks Pattern
```typescript
// hooks/useAuth.ts
import { useContext } from 'react';
import { AuthContext } from '@/context/AuthContext';

export const useAuth = () => {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
};
```

### Context Pattern
```typescript
// context/AuthContext.tsx
import React, { createContext, useReducer, ReactNode } from 'react';

interface User {
  id: number;
  email: string;
  role: string;
}

interface AuthState {
  user: User | null;
  isLoading: boolean;
  error: string | null;
}

interface AuthContextType extends AuthState {
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
}

export const AuthContext = createContext<AuthContextType | undefined>(undefined);

export const AuthProvider: React.FC<{ children: ReactNode }> = ({ children }) => {
  const [state, dispatch] = useReducer(authReducer, initialState);

  // Implementation...

  return (
    <AuthContext.Provider value={{ ...state, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
};
```

---

## Чеклист входных критериев по фазам

### Перед началом каждой фазы проверить:
- [ ] Соответствующие Backend endpoints готовы и задокументированы
- [ ] API контракт согласован (типы данных, структура ответов)
- [ ] Frontend окружение настроено (зависимости установлены)
- [ ] Предыдущие фазы завершены и протестированы
- [ ] Design mockups/wireframes готовы (если есть)

---

## Примерный график разработки

| Фаза | Название | Дни | Начало | Конец |
|------|----------|------|--------|-------|
| 0 | Инициализация | 1-2 | 10 фев | 11 фев |
| 1 | Аутентификация | 3-4 | 12 фев | 15 фев |
| 2-3 | Управление клубом, спортсмены | 6-8 | 16 фев | 23 фев |
| 4-5 | Документы, посещения | 5-6 | 24 фев | 01 мар |
| 6 | Финансы | 4-5 | 02 мар | 06 мар |
| 7 | Турнирная система | 7-9 | 07 мар | 15 мар |
| 8 | Аналитика | 4-5 | 16 мар | 20 мар |
| 9 | Системные функции | 3-4 | 21 мар | 24 мар |
| 10 | Тестирование | 3-4 | 25 мар | 28 мар |
| **ИТОГО** | | **40-45 дней** | | |

---

## Версия документа

- **Версия:** 1.0
- **Дата создания:** 9 февраля 2026
- **Статус:** ✅ READY FOR DEVELOPMENT
- **Обновлено:** 9 февраля 2026

---

## Следующие шаги

1. ✅ Ознакомиться с этим планом жизнедеятельности
2. ✅ Убедиться, что Backend Stage 1 полностью завершена
3. ✅ Начать **Фазу 0: Инициализация фронтенда**
4. Создать issues в GitHub для каждой фазы
5. Отслеживать прогресс еженедельно

---

**Готово к запуску разработки фронтенда!** 🚀
